apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.appName }}
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.appReplicas }}
  selector:
    matchLabels:
      app: {{ .Values.appName }}
  template:
    metadata:
      labels:
        app: {{ .Values.appName }}
    spec:
      containers:
      - name: {{ .Values.appName }}
        image: "{{ .Values.appImage }}:{{ .Values.appVersion }}"
        ports:
        - containerPort: {{ .Values.containerPort }}
        env:
        {{- range .Values.containersEnvVars }}
        - name: {{ .name }}
          value: "{{ .value }}"
        {{- end }}

        {{- if .Values.healthCheck.enabled }}
        readinessProbe:
          periodSeconds: {{ .Values.healthCheck.readiness.periodSeconds | default 5 }}
          initialDelaySeconds: {{ .Values.healthCheck.readiness.initialDelaySeconds | default 10 }}
          {{- if eq .Values.healthCheck.type "grpc" }}
          grpc:
            port: {{ .Values.healthCheck.port | default .Values.containerPort }}
          {{- else if eq .Values.healthCheck.type "http" }}
          httpGet:
            path: {{ .Values.healthCheck.path | default "/health" }}
            port: {{ .Values.healthCheck.port | default .Values.containerPort }}
          {{- end }}
        livenessProbe:
          periodSeconds: {{ .Values.healthCheck.liveness.periodSeconds | default 5 }}
          initialDelaySeconds: {{ .Values.healthCheck.liveness.initialDelaySeconds | default 30 }}
          {{- if eq .Values.healthCheck.type "grpc" }}
          grpc:
            port: {{ .Values.healthCheck.port | default .Values.containerPort }}
          {{- else if eq .Values.healthCheck.type "http" }}
          httpGet:
            path: {{ .Values.healthCheck.path }}
            port: {{ .Values.healthCheck.port | default .Values.containerPort }}
          {{- end }}
        {{- end }}
        resources:
          requests:
            cpu: {{ .Values.requestsCpu}}
            memory: {{ .Values.requestsMemory}}
          limits:
            cpu: {{ .Values.limitsCpu}}
            memory: {{ .Values.limitsMemory}}